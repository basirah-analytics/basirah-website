# local project brief + tooling config (kept on disk, not published)
CLAUDE.md
.claude/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# editor
.vscode/
.idea/
*.swp

# Working brief, must never be served publicly by the Worker.
SCHEMA-FIXES.md

# Design skills installed by npx. Kept on disk, never published: the Worker
# serves repo files publicly, and these are third-party instruction files.
.agents/
skills-lock.json

# Impeccable product record, design system and config: internal, never published.
PRODUCT.md
DESIGN.md
.impeccable/
